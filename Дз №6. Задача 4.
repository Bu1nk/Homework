numbers = input().split()

for i in range(len(numbers)-2+1):
    pair = numbers[i:i+2]
    if (int(pair[0]) * int(pair[1])) > 0:
        print(*pair)
        print(max(int(pair[0]), int(pair[1])))
        break
